# Workspace
All projects share one architecture defined in `_platform/` — the single source of truth.
Never copy platform docs into a project; reference them.
The tier is set by the folder a project lives in: `prototypes/` or `production/`.

@_platform/principles.md
