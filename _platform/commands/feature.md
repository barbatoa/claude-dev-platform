---
description: Implement one feature from the plan
argument-hint: <feature name or number>
---
Implement feature `$ARGUMENTS` from `docs/PLAN.md`.

1. Read only that feature's section and the files it touches. Read a `reference/` doc only if the feature uses that module.
2. Implement the minimum code meeting the acceptance criteria. Follow the principles.
3. Add or update tests; run lint and tests until they pass.
4. Commit on a branch named after the feature.
5. Reply in under 10 lines: what changed, how to try it, anything that needs my decision.
Then suggest `/clear` before the next feature.
